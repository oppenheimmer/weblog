node_modules/
dist/
.DS_Store
*.log
.vercel

# Local environment files. Variable names are documented in CLAUDE.md §6;
# nothing env-related belongs in the repo.
.env
.env.*

# Private planning / agent context. Holds the full authoring-system design:
# endpoint contract, session and cookie configuration, rate-limit thresholds,
# lockout recovery, and review notes on known weaknesses. This repository is
# public, so none of it belongs in a commit.
CLAUDE.md
AGENTS.md
PLAN.md
.env*
